@extends('capstone::layouts.app')
@section('title','Supervisor Evaluation')
@section('content')
<div x-data="lecturerSupervisorEvaluations" class="space-y-6">
    <div class="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><h1 class="text-3xl font-bold tracking-tight">Supervisor Evaluation</h1><p class="text-muted-foreground">Evaluate the progress and performance of your supervised students.</p></div>@include('capstone::pages.dosen.shared.period')</div>
    <div class="flex flex-wrap gap-3"><x-capstone::input type="search" x-model.debounce.200ms="search" @input="page=1" placeholder="Search groups or students..." aria-label="Search supervisor evaluations" class="min-w-48 flex-1" /><select x-model="type" @change="page=1" aria-label="Evaluation type" class="h-9 rounded-md border bg-background px-3 text-sm"><option value="all">All Types</option><template x-for="(label,key) in evaluationLabels" :key="key"><option :value="key" x-text="label"></option></template></select><select x-model="status" @change="page=1" aria-label="Evaluation status" class="h-9 rounded-md border bg-background px-3 text-sm"><option value="all">All Statuses</option><option value="PENDING">Pending</option><option value="PARTIAL">Partial</option><option value="COMPLETED">Completed</option><option value="NOT_CONFIGURED">Not Configured</option></select></div>
    <div class="inline-flex rounded-lg bg-muted p-1" role="group" aria-label="Display evaluations by"><button type="button" @click="view='groups'" :aria-pressed="view==='groups'" :class="view==='groups'?'bg-background shadow-sm':''" class="rounded-md px-4 py-2 text-sm font-medium">By Group</button><button type="button" @click="view='schedules'" :aria-pressed="view==='schedules'" :class="view==='schedules'?'bg-background shadow-sm':''" class="rounded-md px-4 py-2 text-sm font-medium">By Schedule</button></div>
    @include('capstone::partials.loading')
    <div x-show="!loading && !error" x-cloak>
        <div x-show="view==='groups'" class="space-y-4"><p x-show="!filtered.length" class="rounded-xl border border-dashed p-12 text-center text-muted-foreground">No matching supervisor evaluations.</p>
            <div class="grid gap-4 lg:grid-cols-2"><template x-for="group in visible" :key="group.id"><article class="rounded-xl border bg-card p-6 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-2"><h2 class="text-lg font-semibold" x-text="group.code||group.name||'Group '+group.id"></h2><span class="rounded bg-accent px-2 py-1 text-xs text-primary" x-text="group.supervisor_role?.replaceAll('_',' ')"></span></div><p class="mt-2 text-xs text-muted-foreground" x-text="group.period?.name"></p><p class="mt-3 text-sm" x-text="members(group)"></p>
                <div class="mt-4 space-y-3"><template x-for="entry in entries(group)" :key="entry[0]"><div class="rounded-lg border p-4"><div class="flex items-center justify-between gap-2"><h3 class="text-sm font-medium" x-text="evaluationLabels[entry[0]]||entry[0]"></h3><span class="rounded-full px-2 py-1 text-xs" :class="statusColor(entry[1].status.toUpperCase())" x-text="entry[1].status"></span></div><p class="mt-2 text-xs text-muted-foreground" x-text="date(entry[1].date)+' · '+(entry[1].room||'-')"></p><p x-show="entry[1].deadline" class="mt-1 text-xs text-muted-foreground" x-text="'Deadline: '+date(entry[1].deadline,true)"></p>
                    <div class="mt-3 flex gap-2" x-show="entry[1].status.toUpperCase()!=='NOT_CONFIGURED'"><a :href="evaluationUrl(group.id,entry[0],entry[1].status.toUpperCase()==='COMPLETED'||entry[1].editable===false)" class="rounded-md bg-primary px-3 py-2 text-xs font-medium text-primary-foreground" x-text="entry[1].status.toUpperCase()==='COMPLETED'||entry[1].editable===false?'View Scores':'Evaluate'"></a><a x-show="entry[1].status.toUpperCase()==='COMPLETED'&&entry[1].editable!==false" :href="evaluationUrl(group.id,entry[0])" class="rounded-md border px-3 py-2 text-xs font-medium">Edit</a></div>
                    <p x-show="entry[1].editable===false" class="mt-3 text-xs text-muted-foreground">Submissions are closed for the group's current status.</p>
                    <p x-show="entry[1].status.toUpperCase()==='NOT_CONFIGURED'" class="mt-3 text-xs text-muted-foreground">Assessment components have not been configured.</p>
                </div></template></div>
            </article></template></div>
            @include('capstone::pages.dosen.shared.pagination')
        </div>
        <div x-show="view==='schedules'" class="overflow-x-auto rounded-xl border"><table class="w-full min-w-[780px] text-left text-sm"><thead class="border-b bg-muted/40"><tr>@foreach(['Group / Student','Evaluation','Schedule','Status','Action'] as $label)<th class="p-4">{{ $label }}</th>@endforeach</tr></thead><tbody><template x-for="(item,index) in filteredSchedules" :key="index"><tr class="border-b last:border-0"><td class="p-4"><p class="font-medium" x-text="item.group?.code||item.group?.name||'Group '+item.group?.id"></p><p class="mt-1 text-xs text-muted-foreground" x-text="item.student?.name||(item.students||[]).map(s=>s.name).join(', ')"></p></td><td class="p-4" x-text="evaluationLabels[item.evaluation_type]||item.evaluation_type"></td><td class="p-4"><p x-text="date(item.date)"></p><p class="mt-1 text-xs text-muted-foreground" x-text="item.room||'-'"></p></td><td class="p-4"><span class="rounded-full px-2 py-1 text-xs" :class="statusColor(item.status)" x-text="item.status"></span></td><td class="p-4"><a x-show="item.status!=='NOT_CONFIGURED'" :href="evaluationUrl(item.group.id,item.evaluation_type,item.status==='COMPLETED',item.student?.id)" class="rounded-md bg-primary px-3 py-2 text-xs font-medium text-primary-foreground" x-text="item.status==='COMPLETED'?'View Scores':'Evaluate'"></a></td></tr></template></tbody></table><p x-show="!filteredSchedules.length" class="p-12 text-center text-muted-foreground">No matching schedules.</p></div>
    </div>
</div>
@endsection
